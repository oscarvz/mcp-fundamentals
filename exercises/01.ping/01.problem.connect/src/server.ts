// 💰 you're gonna want these imports
// import { serve } from '@hono/node-server'
// import { app } from './index.js'

// 🐨 get the PORT from environment variable or default to 3000
// 💰 const PORT = process.env.PORT ? parseInt(process.env.PORT) : 3000

// 🐨 use serve() from @hono/node-server to start the server
// 💰 pass it an object with { fetch: app.fetch, port: PORT }

// 🐨 add a log (using console.error) to let the user know the server is running
// 💰 console.error(`EpicMe MCP Server running on http://localhost:${PORT}`)

// 💣 you can delete this once you're done
throw new Error('Not implemented')
